import { Inter, Lusitana } from 'next/font/google';

export const inter = Inter({ subsets: ['latin'], variable: '--font-sans' });
export const lusitana = Lusitana( { subsets: ['latin'], variable: '--font-sans' , weight: ['400', '700'] });